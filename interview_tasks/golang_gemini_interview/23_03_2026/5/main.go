package main

// Суть задачи:
// У тебя есть входной канал с задачами. Задачи тяжелые, поэтому их нужно обрабатывать параллельно (используя N воркеров). Однако результаты в выходной канал должны поступать строго в том же порядке, в котором задачи были получены из входного канала.
// Пример:
// Из канала in пришла Задача №1, следом Задача №2.
// Задача №1 сложная и обрабатывается 5 секунд.
// Задача №2 простая и обрабатывается 1 секунду.
// Несмотря на то, что Задача №2 готова раньше, её результат не должен попасть в выходной канал раньше результата Задачи №1.
// Требования:
// Параллелизм: n воркеров.
// Порядок: результаты в выходном канале должны соответствовать порядку задач во входном.
// Эффективность: воркеры не должны простаивать. Если Задача №1 затянулась, остальные воркеры могут продолжать обрабатывать Задачи №2, №3 и т.д., но их результаты должны "ждать" своей очереди на отправку.
// Отсутствие утечек памяти: нельзя просто накопить миллион результатов в мапе, если их не забирают из выходного канала.

type Task func() string

func ProcessOrdered(in <-chan Task, n int) <-chan string {
    // Твой код здесь
}